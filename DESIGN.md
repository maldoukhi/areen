# DESIGN.md — هوية عرين | Areen

نظام التصميم لمنصة «عرين» — تطبيق ويب جوال أولًا (PWA) لعرض وإدارة جداول التمارين. هذا الملف مرجع مُلزِم — أي مكوّن جديد يتبعه حرفيًا.

---

## 1. المبدأ

**داكن، حاد، عالي التباين، والرقم هو البطل.**

المتدرب يفتح الجدول وهو واقف في النادي، بيد واحدة، وأحيانًا وهو يلهث. لذلك: هدف واحد لكل شاشة، الأرقام كبيرة وواضحة، والمسافات كريمة. الديكور عدو.

---

## 2. الألوان

### Ink — السطوح والنصوص (مشتق من `#1A2E34`)

| Token | Hex | الاستخدام |
|---|---|---|
| `ink-50` | `#F2F5F6` | خلفية الوضع الفاتح |
| `ink-100` | `#E2E9EB` | |
| `ink-200` | `#C4D1D5` | نص ثانوي على داكن |
| `ink-300` | `#9BB0B6` | نص خافت على داكن |
| `ink-400` | `#6E8991` | تسميات، أيقونات معطّلة |
| `ink-500` | `#506C75` | |
| `ink-600` | `#3E565E` | حدود الأزرار الثانوية |
| `ink-700` | `#33474D` | الحد الشعري على داكن |
| `ink-800` | `#24393F` | سطح مرتفع (بطاقة، صف) |
| `ink-900` | `#1A2E34` | **السطح الأساسي** |
| `ink-950` | `#101F24` | خلفية الصفحة |

### Brand — التمييز (مشتق من `#61B5D1`)

| Token | Hex | الاستخدام |
|---|---|---|
| `brand-50` | `#F0F9FC` | |
| `brand-100` | `#DBF0F7` | |
| `brand-200` | `#BAE2EF` | |
| `brand-300` | `#8ECDE2` | |
| `brand-400` | `#61B5D1` | **الأساسي** — أزرار، أرقام، روابط، حالة نشطة |
| `brand-500` | `#4098B8` | hover |
| `brand-600` | `#337B9B` | |
| `brand-700` | `#2C637D` | |
| `brand-800` | `#2A5367` | |
| `brand-900` | `#274657` | |
| `brand-950` | `#152D3A` | **نص فوق `brand-400`** |

### دلالية

| Token | Hex | الاستخدام |
|---|---|---|
| `ember` | `#F2994A` | رقم قياسي، «اليوم»، سلسلة التزام. **أقل من ٥٪ من الشاشة** |
| `success` | `#34D399` | تمرين مكتمل |
| `danger` | `#F87171` | حذف، خطأ |
| `warning` | `#FBBF24` | تنبيه |

### قواعد اللون

- **الوضع الداكن هو الأساس.** الفاتح نسخة ثانوية (وللطباعة).
- النص فوق `brand-400` دائمًا `brand-950` — **لا أبيض ولا أسود**.
- الارتفاع بالسطح لا بالظل: `ink-950` (الصفحة) → `ink-900` (سطح) → `ink-800` (بطاقة) → حد `ink-700`.
- `ember` يظهر في مكان واحد لكل شاشة كحد أقصى.
- التباين: نص المتن ≥ 4.5:1، العناوين ≥ 3:1. تحقّق من كل تركيبة.

---

## 3. الخطوط

**عائلة واحدة للغتين: IBM Plex Sans Arabic** — تغطي العربية واللاتينية بمقاييس متطابقة، ٧ أوزان، مفتوحة المصدر. تُحمّل محليًا من `public/fonts/` بصيغة woff2 مع `font-display: swap`.

بديلان مقبولان: **Tajawal** (أدفأ) أو **Zain** (أحدّ).

| الدور | الحجم | الوزن | ارتفاع السطر |
|---|---|---|---|
| Display | `clamp(2.25rem, 5vw, 3rem)` | 700 | 1.15 |
| H1 | 2rem | 700 | 1.25 |
| H2 | 1.5rem | 600 | 1.3 |
| H3 | 1.25rem | 600 | 1.4 |
| Body | 1rem | 400 | **1.75** |
| Small | 0.875rem | 400 | 1.6 |
| Caption | 0.75rem | 500 | 1.5 |
| Metric | 1.5–2.5rem | 700 | 1 |

### قواعد الخط

- **`letter-spacing: 0` مع العربية دائمًا.** التتبّع يفكّك الحروف المتصلة. التتبّع السالب (`-0.02em`) للعناوين اللاتينية فقط.
- ارتفاع سطر العربية أعلى من اللاتينية — لا تنزل تحت 1.7 في المتن.
- كل الأرقام: `font-variant-numeric: tabular-nums` حتى لا ترقص عند التحديث.
- **لا حروف كبيرة كليًا (ALL CAPS)** في اللاتينية داخل سياق عربي — تكسر الإيقاع البصري.
- أرقام **غربية (4×10)** في كل الواجهة — أسرع قراءة على الجوال وأقل مشاكل محاذاة.

---

## 4. الشكل والمسافة

- **الوحدة الأساسية 4px.** السلّم: 4 · 8 · 12 · 16 · 24 · 32 · 48 · 64.
- **الأنصاف القطرية:** `sm 8px` (أزرار، حقول) · `md 12px` (صفوف، وسائط) · `lg 16px` (بطاقات) · `xl 24px` (أقسام) · `full` (رقائق).
- **الحدود:** `1px` فقط. `ink-700` على داكن، `ink-200` على فاتح.
- **الظلال:** ممنوعة على الداكن. الاستثناء الوحيد حلقة التركيز: `0 0 0 3px` بـ `brand-400/40`.
- **العرض الأقصى:** حاوية 1200px، عمود النص 65ch.
- **الشبكة:** جوال أولًا — عمود واحد → عمودان عند 640px → ثلاثة عند 1024px.

---

## 5. المكوّنات

| المكوّن | المواصفة |
|---|---|
| زر أساسي | خلفية `brand-400`، نص `brand-950`، `radius 8`، `padding 10px 18px`، hover `brand-500` |
| زر ثانوي | شفاف، حد `ink-600`، نص `ink-200`، hover خلفية `ink-800` |
| زر شبح | شفاف بلا حد، hover خلفية `ink-800` |
| بطاقة | `ink-800` + حد `ink-700` + `radius 16` + `padding 20`، hover الحد → `brand-400/40` |
| رقاقة عضلة | حبة، `ink-800`/`ink-300`؛ نشطة `brand-400`/`brand-950` |
| صف تمرين | شارة رقم مربعة (tabular) · الاسم · الراحة بأيقونة ساعة · **جولات×عدات بحجم 20px ولون `brand-400`** · مصغّرة يوتيوب 16:9 |
| تبويب اليوم | شريط مقسّم أفقي قابل للتمرير؛ النشط بخط سفلي `2px brand-400` |
| حقل إدخال | `ink-900` + حد `ink-700`، تركيز حد `brand-400` + حلقة |
| جدول الأدمن | صفوف بحدود سفلية `ink-800`، بلا حواف مدوّرة للصفوف، hover `ink-800/50` |
| حالة فارغة | علامة عرين بشفافية ٦٪ + عنوان دعوة + زر واحد |
| شريط سفلي للجوال | ثابت أسفل صفحة اليوم: «سجّل الجولة» + مؤقّت الراحة |

---

## 6. الأيقونات والوسائط والحركة

- **الأيقونات:** Lucide، سُمك 1.5، مقاس 20px داخل النص و24px مستقل. خطّية فقط — لا خلط مع المصمتة.
- **صور التمارين:** نسبة 4:3، خلفية `ink-800`، `radius 12`، `loading="lazy"`. GIF يعمل تلقائيًا صامتًا ومكرّرًا.
- **يوتيوب:** مصغّرة ثابتة + زر تشغيل، والـ iframe يُحمّل عند النقر فقط (أداء + خصوصية).
- **الصور البطلة:** ثنائية اللون `ink-900`/`brand-400`.
- **الحركة:** 150ms `ease-out` للتحويم، 250ms للألواح، `wire:transition` لتبديل Livewire. احترم `prefers-reduced-motion`.

---

## 7. الشعارات

هناك شعاران منفصلان — لا تخلط بينهما:

### شعار المنصة — عرين

- الملف: `public/brand/areen-mark.svg` · مكوّن Blade: `<x-brand.mark/>` (يرث اللون من `currentColor`).
- الشكل: قوس مفتوح (العرين) وداخله ثلاثة أعمدة تصعد من اليمين لليسار (اتجاه القراءة).
- يُستخدم في: الأيقونات، الحالات الفارغة، الهيدر عند غياب شعار النادي.
- الحد الأدنى: 24px. ممنوع تدويره أو إضافة ظل أو تغيير نسبه.

### شعار النادي

- **لا يُكتب في الكود إطلاقًا.** يُرفع من لوحة الأدمن ويُقرأ من `settings.logo_path`.
- شعار قسورة الأزرق (العميل الأول) يوضع في `public/brand/logo-qaswarah.png` كقيمة ابتدائية للـ seeder فقط.
- الحد الأدنى: 40px ارتفاعًا. مساحة الأمان حوله = ٢٥٪ من ارتفاعه.
- يوضع على `ink-900` أو `ink-950` أو أبيض فقط. **ممنوع على `brand-400`** أو أي صورة.
- ممنوع: تدويره، تغيير ألوانه، إضافة ظل، أو فصل الأسد عن الدائرة.

---

## 8. الطباعة و PDF

- خلفية بيضاء، نص `#111`، بلا ألوان مصمتة كبيرة (توفير حبر).
- الشعار أعلى اليمين + اسم النادي + اسم البرنامج + تاريخ الطباعة.
- جدول واحد لكل يوم، وكل يوم يبدأ في صفحة جديدة (`break-before: page`).
- عمود فارغ «الوزن المستخدم» ليكتب فيه المتدرب بالقلم.
- رمز QR أسفل الصفحة يعيده للنسخة الإلكترونية.
- `@media print` يخفي: التنقل، الأزرار، الفيديو، الفوتر.

---

## 9. اللهجة والنصوص

- عربي واضح مباشر، بلا رطانة. **جولات** لا «مجموعات». **عدات** لا «تكرارات». **راحة** · **تسخين** · **إحماء** · **تبريد**.
- الأزرار تبدأ بفعل: «سجّل الجولة» · «ابدأ اليوم» · «أضف تمرين». لا «موافق» ولا «إرسال».
- الأخطاء: ماذا حدث ثم ماذا تفعل. «رابط اليوتيوب غير صالح. الصق الرابط الكامل.»
- ممنوع علامات التعجب في نصوص النظام.
- الحالات الفارغة دعوة لا اعتذار: «ابدأ ببرنامجك الأول» لا «لا يوجد شيء بعد».

---

## 10. Tailwind 4 — كتلة `@theme`

في `resources/css/app.css`:

```css
@import "tailwindcss";

@theme {
  --color-ink-50:  #F2F5F6;
  --color-ink-100: #E2E9EB;
  --color-ink-200: #C4D1D5;
  --color-ink-300: #9BB0B6;
  --color-ink-400: #6E8991;
  --color-ink-500: #506C75;
  --color-ink-600: #3E565E;
  --color-ink-700: #33474D;
  --color-ink-800: #24393F;
  --color-ink-900: #1A2E34;
  --color-ink-950: #101F24;

  --color-brand-50:  #F0F9FC;
  --color-brand-100: #DBF0F7;
  --color-brand-200: #BAE2EF;
  --color-brand-300: #8ECDE2;
  --color-brand-400: #61B5D1;
  --color-brand-500: #4098B8;
  --color-brand-600: #337B9B;
  --color-brand-700: #2C637D;
  --color-brand-800: #2A5367;
  --color-brand-900: #274657;
  --color-brand-950: #152D3A;

  --color-ember: #F2994A;

  --font-sans: "IBM Plex Sans Arabic", system-ui, sans-serif;

  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-xl: 24px;
}
```

**تذكير:** لا يوجد `tailwind.config.js` في Tailwind 4 — كل الإعداد هنا.

---

## 11. الجوال ووضع التطبيق (standalone)

### الإيقاع البصري على الجوال

- صمّم على **360px** أولًا. لو لم تنجح الشاشة هناك، لم تنجح.
- هامش الصفحة 16px، وبين البطاقات 12px، وبين الأقسام 32px.
- منطقة اللمس **≥ 44×44px** — حتى لو كانت الأيقونة 20px.
- **الإجراء الأساسي في النصف السفلي** من الشاشة. الإبهام لا يصل للأعلى بيد واحدة.
- عنوان الصفحة يبقى ظاهرًا عند التمرير (sticky) بارتفاع 56px وخلفية `ink-950/90` مع `backdrop-blur`.

### المناطق الآمنة

كل عنصر ثابت يحترم نتوء الشاشة وشريط الإيماءات:

```html
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
```

```css
padding-bottom: calc(16px + env(safe-area-inset-bottom));
padding-top:    calc(12px + env(safe-area-inset-top));
```

في هذا المشروع الأداتان `safe-pb` و `safe-pt` معرّفتان في `resources/css/app.css`.

### شريط التنقّل السفلي

يظهر **فقط** في وضع standalone (`@media (display-mode: standalone)`):

- ارتفاع 60px + المنطقة الآمنة، خلفية `ink-900`، حد علوي `ink-800`.
- أربعة عناصر: الرئيسية · البرامج · تمريني · حسابي.
- النشط: أيقونة + نص بـ `brand-400`. غير النشط: `ink-400`.
- النص 11px تحت الأيقونة 22px.

### صفحة اليوم — الشاشة الأهم

- التبويبات أفقية قابلة للتمرير + **swipe** للانتقال بين الأيام.
- كل تمرين صف واحد: رقم · اسم · جولات×عدات بـ `brand-400` حجم 20px · مصغّرة فيديو.
- **شريط إجراء ثابت أسفل**: «سجّل الجولة» (`brand-400`) + مؤقّت الراحة.
- المؤقّت أثناء العمل: رقم بحجم 48px، `tabular-nums`، بلون `ember`، ودائرة تقدّم حوله.
- انتهاء الراحة: اهتزاز (Vibration API) + نغمة قصيرة + وميض واحد للشريط. **لا مودال**.
- مفتاح «إبقاء الشاشة مضاءة» (Wake Lock) في أعلى الصفحة.

### حالة الاتصال

- شريط رفيع أعلى الصفحة عند انقطاع الشبكة: خلفية `ember`، نص `#4A2A08`، «غير متصل — سيُحفظ ويُرسل لاحقًا».
- السجلات غير المزامَنة تحمل نقطة `ember` صغيرة بجانبها حتى تُزامَن.
- **لا تعطّل أي زر** بسبب انقطاع الشبكة — اقبل الإدخال واحفظه محليًا.

### أصول PWA

| الأصل | المواصفة |
|---|---|
| `icon-192.png` / `icon-512.png` | علامة عرين على `#1A2E34` |
| `icon-maskable-512.png` | العلامة داخل دائرة أمان بنسبة ٨٠٪ — الحواف تُقصّ على أندرويد |
| `apple-touch-icon.png` 180 | بلا شفافية، حواف مربعة (iOS يدوّرها) |
| شاشة الإقلاع | خلفية `#101F24` + العلامة في المنتصف بعرض 40٪ |
| `theme_color` | `#1A2E34` |
| `background_color` | `#101F24` |

### كشف الوضع

```css
@media (display-mode: standalone) { /* شريط سفلي، إخفاء الفوتر والروابط الخارجية */ }
@media (hover: none)             { /* لا تعتمد على :hover إطلاقًا */ }
```

---

## 12. قائمة تحقّق لكل شاشة

- [ ] هدف واحد واضح، وزر أساسي واحد فقط
- [ ] سليمة في RTL و LTR (خصائص منطقية فقط: `ms-` `me-` `ps-` `pe-` `start-` `end-`)
- [ ] تعمل على 360px، ومنطقة اللمس ≥ 44px
- [ ] الإجراء الأساسي في النصف السفلي من الشاشة
- [ ] العناصر الثابتة تحترم `safe-area-inset`
- [ ] لا وظيفة تعتمد على `:hover`
- [ ] الشاشة مفهومة ومفيدة بدون إنترنت
- [ ] الأرقام بـ `tabular-nums`
- [ ] لا نص ثابت خارج `__()`
- [ ] حالة فارغة وحالة تحميل وحالة خطأ مصمّمة
- [ ] تباين مفحوص، وتركيز لوحة المفاتيح ظاهر
- [ ] `ember` غير مستخدم أكثر من مرة
